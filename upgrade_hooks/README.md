# Upgrade Hooks

This folder contains lightweight stubs for future research extensions.

They do not connect to external systems yet. Each stub prints or writes a small example payload showing the contract that a real integration should satisfy later.

The full extension plan is documented in:

```text
docs/upgrades.md
```
